<div class="{{ $fieldClass }}">
    <label for="{{ $inputId }}" class="{{ $labelClass }}">{{ $labelValue }}</label>
    <input 
        type="{{ $inputType }}" 
        value="{{ $inputValue }}" 
        name="{{ $inputName }}" 
        id="{{ $inputId }}" 
        class="{{ $inputClass }}"
    >
</div>
